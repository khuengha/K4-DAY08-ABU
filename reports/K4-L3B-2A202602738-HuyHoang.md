# Individual contribution report

## Thông tin

- Họ và tên: Nguyễn Huy Hoàng
- Mã học viên: 2A202602738
- Nhóm: ABU
- Repository/branch: [K4-L3B-RAG-Pipeline / hoang](https://github.com/khuengha/K4-L3B-RAG-Pipeline/tree/hoang)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 3 — Chuẩn hóa dữ liệu | Chuyển HTML/JSON từ Task 1, 2 thành 11 file Markdown (3 legal, 8 news); loại nhiễu và mục rỗng, giữ bảng, các bộ ngọc/phù hiệu/phép bổ trợ và metadata nguồn; kiểm tra dữ liệu rỗng, trùng tên và chạy lại không ghi file thừa. | [task3_convert_markdown.py](../src/task3_convert_markdown.py), commit [5e0fafa](https://github.com/khuengha/K4-L3B-RAG-Pipeline/commit/5e0fafa) | Done |
| Task 6 — Lexical search | Triển khai BM25+ trên cùng chunks ChromaDB với Task 5; chuẩn hóa Unicode và token; cache index; trả SearchResult giữ ID/metadata, không trùng, sắp điểm giảm dần và giới hạn top_k. | [task6_lexical_search.py](../src/task6_lexical_search.py), commit [8b2b03c](https://github.com/khuengha/K4-L3B-RAG-Pipeline/commit/8b2b03c) | Done |
| Task 9 — Retrieval pipeline | Kết nối dense, lexical và RRF; fuse một lần; quyết định PageIndex fallback bằng cosine score gốc; giữ kết quả chính khi fallback lỗi/rỗng; xử lý query rỗng và chế độ tắt reranking. | [task9_retrieval_pipeline.py](../src/task9_retrieval_pipeline.py), commit [87228eb](https://github.com/khuengha/K4-L3B-RAG-Pipeline/commit/87228eb) | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng BM25+ trên chunks đã index, giữ nguyên ID.  
   **Lý do/evidence:** Bảo đảm lexical và dense dùng cùng corpus để ghép bằng RRF; tránh IDF bằng 0 trên corpus nhỏ. Chỉ trả chunks có token thực sự trùng truy vấn để loại điểm nền BM25+.  
   **Trade-off:** Mỗi truy vấn vẫn đọc toàn bộ corpus; tokenizer chưa hỗ trợ từ đồng nghĩa hoặc tìm kiếm không dấu.

2. **Quyết định:** Dùng cosine score gốc, không dùng điểm RRF, để quyết định fallback.  
   **Lý do/evidence:** Hai thang điểm khác nhau; Task 9 so sánh cosine score tốt nhất với threshold mặc định `0.3`, giữ kết quả chính nếu PageIndex lỗi/rỗng.  
   **Trade-off:** Threshold chưa được hiệu chỉnh bằng đánh giá riêng; kết quả giữ lại chưa chắc đủ liên quan để trả lời.

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:** `python -m pytest tests/test_contracts.py -q`; `python -m pytest tests/test_task3_convert_markdown.py tests/test_acceptance.py::test_standardized_output_covers_both_source_types -q`; `python -m src.task6_lexical_search Valhein --top-k 2`.
- **Kết quả trước/sau nếu có:** Lần kiểm tra khi lập báo cáo ghi nhận **15 contract tests passed**; nhóm test Task 3 và đầu ra chuẩn hóa đạt **17 passed, 1 failed**. Query Valhein trả hai chunks có ID, metadata và điểm BM25. Contract tests dùng dữ liệu/provider giả; chưa có số liệu A/B riêng để kết luận cải thiện chất lượng retrieval.
- **Lỗi đã phát hiện và cách xử lý:** Corpus legal hiện có nhiều định dạng cùng tên gốc, gây lỗi `Duplicate legal stems`. Task 3 chủ động dừng để tránh ghi đè; việc xử lý trùng tên trong dữ liệu đầu vào vẫn chưa hoàn tất tại thời điểm lập báo cáo.

## Điều còn hạn chế

- **Một hạn chế cụ thể của phần tôi làm:** Task 3 chưa chuyển lại được toàn bộ landing corpus hiện tại do trùng tên nguồn legal; nhánh PDF/DOCX mới kiểm thử dispatch bằng converter giả.
- **Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện:** Thống nhất tên/chọn định dạng nguồn để chạy lại Task 3 ổn định; sau đó đánh giá trên golden dataset để hiệu chỉnh threshold và so sánh dense-only với hybrid.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Nguyễn Huy Hoàng
